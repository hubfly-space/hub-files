# HubFly Files Demo

This folder is used by HubFly Files when you open it in demo mode.

Quick notes:

- demo mode is read-only
- uploads, edits, deletes, rename, zip, and extract are disabled
- add a few sample files here if you want people to see example content

If you want real file access, create a normal session through the management API instead of using demo mode.
